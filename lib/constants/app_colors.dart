import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.black;
  static const Color secondary = Color(0xFFDEAB02);
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Color(0xFF828282);
  static const Color textPlaceholder = Color(0xFF979797);
  static const Color background = Colors.white;
  static const Color surface = Color(0xFFF7F7F7);
  static const Color border = Color(0x33000000); // Colors.black.withValues(alpha: 0.20)
  static const Color borderLight = Color(0x1A000000); // Colors.black.withValues(alpha: 0.10)
  static const Color inactive = Color(0x66000000); // Colors.black.withValues(alpha: 0.40)
  static const Color shadow = Color(0x33000000); // Colors.black.withOpacity(0.20)
}